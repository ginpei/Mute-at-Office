<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Mute_at_Office.Pages.Dashboard.SafeZoneItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mute_at_Office.Pages.Dashboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Button
        HorizontalAlignment="Stretch"
        HorizontalContentAlignment="Stretch"
        Padding="0"
        BorderThickness="0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="32"/>
            </Grid.ColumnDefinitions>
            <Grid Padding="12" RowSpacing="8" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0">Speaker</TextBlock>
                <TextBlock Grid.Row="0" Grid.Column="1" TextWrapping="Wrap">...</TextBlock>
                <TextBlock Grid.Row="1" Grid.Column="0">SSID</TextBlock>
                <TextBlock Grid.Row="1" Grid.Column="1" TextWrapping="Wrap">...</TextBlock>
            </Grid>
            <FontIcon
                Grid.Column="1"
                FontFamily="Segoe MDL2 Assets"
                FontSize="16"
                Glyph="&#xE76C;"/>
        </Grid>
    </Button>
</UserControl>

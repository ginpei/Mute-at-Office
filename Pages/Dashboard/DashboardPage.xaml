<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Mute_at_Office.Pages.Dashboard.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mute_at_Office.Pages.Dashboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="ToggleSwitch">
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Page.Resources>

    <StackPanel Spacing="26" Margin="20,30">
        <TextBlock
            Text="Mute at Office"
            FontSize="28"
            FontWeight="SemiBold"
            />
        <StackPanel Spacing="8">
            <TextBlock Text="Actions" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <StackPanel
                Background="{ThemeResource ControlFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="4"
                >
                <Button
                    Background="Transparent"
                    BorderThickness="0"
                    HorizontalContentAlignment="Stretch"
                    HorizontalAlignment="Stretch"
                    AutomationProperties.Name="Open rule details"
                    Padding="12"
                    >
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" TextWrapping="Wrap">
                            <Run FontWeight="Bold">Revert</Run>
                            <LineBreak/>
                            when
                            <Run FontWeight="Bold">WiFi SSID</Run>
                            is
                            <Run x:Name="SsidRun" FontWeight="Bold">...</Run>
                            <LineBreak/>
                            and
                            <Run FontWeight="Bold">Speaker</Run>
                            is
                            <Run x:Name="SpeakerRun" FontWeight="Bold">...</Run>
                        </TextBlock>

                        <FontIcon Grid.Column="1" FontFamily="Segoe MDL2 Assets" FontSize="16" Glyph="&#xE76C;" HorizontalAlignment="Right"/>
                    </Grid>
                </Button>

                <Border BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"/>

                <Button
                    Background="Transparent"
                    BorderThickness="0"
                    HorizontalContentAlignment="Stretch"
                    HorizontalAlignment="Stretch"
                    AutomationProperties.Name="Open rule details"
                    Padding="12"
                    >
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" TextWrapping="Wrap">
                            <Run FontWeight="Bold">Mute</Run>
                            <LineBreak/>
                            otherwise
                        </TextBlock>

                        <FontIcon Grid.Column="1" FontFamily="Segoe MDL2 Assets" FontSize="16" Glyph="&#xE76C;" HorizontalAlignment="Right"/>
                    </Grid>
                </Button>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>

<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Mute_at_Office.Pages.Settings.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mute_at_Office.Pages.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock
            Text="Settings" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            FontSize="24"
            />
        <StackPanel Orientation="Vertical" Margin="12" Spacing="12">
            <TextBox Header="File path" Text="{x:Bind FilePath, Mode=OneWay}" IsReadOnly="True" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                    <Button
                        Content="Reload"
                        Click="OnReloadClicked"
                        />
                    <!-- TODO show spinner while loading -->
                </StackPanel>
                <Button Grid.Column="1" Content="Reveal in File Explorer" Click="OpenButton_Clicked" />
            </Grid>
        </StackPanel>
    </StackPanel>
</Page>

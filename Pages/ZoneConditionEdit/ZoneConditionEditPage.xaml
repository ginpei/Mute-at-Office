<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Mute_at_Office.Pages.ZoneConditionEdit.ZoneConditionEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mute_at_Office.Pages.ZoneConditionEdit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="26" Margin="20,30">
        <TextBlock
                Text="Edit zone condition"
                FontSize="28"
                FontWeight="SemiBold"
                />
        <StackPanel Spacing="8">
            <TextBlock Text="{x:Bind ZoneCondition.Ssid, Mode=TwoWay}" />
            <TextBlock Text="{x:Bind ZoneCondition.SpeakerName, Mode=TwoWay}" />
        </StackPanel>
    </StackPanel>
</Page>
